programa {
  funcao inicio() {
    inteiro lado1, lado2, lado3
    leia (lado1, lado2, lado3)
    inteiro soma = lado1 + lado2
    se (soma > lado3)
    escreva ("válido")
    senao
    escreva ("inválido")
  }
}
